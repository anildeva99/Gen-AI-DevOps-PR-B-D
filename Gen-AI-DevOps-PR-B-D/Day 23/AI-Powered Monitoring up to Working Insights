AI-Powered Monitoring up to Working Insights

Goal

Set up:

a GCP VM running Prometheus + Grafana + Node Exporter

a Python environment that uses Gemini 2.5 Flash to explain anomalies


Open ports (Prometheus 9090, Grafana 3000, Node Exporter 9100)

gcloud compute firewall-rules create allow-monitoring \
  --allow=tcp:9090,tcp:9100,tcp:3000 \
  --target-tags=http-server


SSH into the VM

gcloud compute ssh ai-monitoring-vm --zone=us-central1-a

Install Base Packages
sudo apt update -y
sudo apt install -y docker.io docker-compose git curl python3 python3-pip
sudo systemctl enable docker
sudo systemctl start docker


Deploy Prometheus + Grafana + Node Exporter

 Create working directory

mkdir ~/monitoring && cd ~/monitoring

vi prometheus.yml

global:
  scrape_interval: 15s
scrape_configs:
  - job_name: 'node-exporter'
    static_configs:
      - targets: ['node-exporter:9100']

Docker compose file 


version: '3'
services:
  prometheus:
    image: prom/prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml

  grafana:
    image: grafana/grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin

  node-exporter:
    image: prom/node-exporter
    ports:
      - "9100:9100"


launch the docker 

sudo docker-compose up -d

verify 
sudo docker ps


Access Dashboards

Prometheus: http://<VM_EXTERNAL_IP>:9090

Grafana: http://<VM_EXTERNAL_IP>:3000
(login → admin / admin)


install Python AI Dependencies
pip3 install --upgrade google-cloud-aiplatform pandas scikit-learn requests
gcloud auth application-default login


Create AI Anomaly Detection Script

vi ai_anomaly.py

Test the ai_anomaly.py 
python3 ai_anomaly.py

Expecte result should be :

Fetching metrics from Prometheus...
Retrieved 16 metrics from Prometheus.
 Running anomaly detection on metrics...
  1 anomalies detected.

 Metric Value: 1311.13
 Gemini Insight:
. Node Restart / Replacement:
   Cloud Context: extremely common due to auto-scaling events …
   ...




